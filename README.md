# Academic homepage

Single-file academic homepage based on [Jon Barron's template](https://github.com/jonbarron/website).

Edit `index.html` (all content + `TODO` markers are in there). Deploy by pushing to a
GitHub repo named `<username>.github.io` and enabling GitHub Pages (Settings → Pages → Deploy from `main`).
